import openai



while True:
    command = input("Enter robot command: ")
    
    # print command
    # use LLM to generate goal and intermediate plans to accomplish set goal
    # print the criticism of the plan
    # make the robot continually execute each plan towards its goal until it acheives its goal
    # get image feedback from the camer
    # examine at each point if the previous plan was accomplished and how does that change the rest of the plans